name: Build
on:
  workflow_dispatch:
    inputs:
      environment:
        type: string
        default: DEV

  push:
    paths-ignore:
      - 'docs/**'

jobs:
#  build:
#    uses: ./.github/workflows/modin.yml

  build-docker:
    uses: ./.github/workflows/build-docker.yml

  test-cuda-docker:
    needs: build-docker
    uses: ./.github/workflows/test-docker-cpu.yml
